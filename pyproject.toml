[project]
name = "ai-dev-environment"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.120.0",
    "loguru>=0.7.3",
    "pydantic>=2.12.3",
    "python-dotenv>=1.1.1",
]
